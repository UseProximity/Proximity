{
  "_description": "Living log of in-progress tasks, architectural decisions, known bugs, and ongoing migrations. Updated via the log-task tool.",
  "tasks": [],
  "decisions": [
    {
      "id": "decision-1780854467717",
      "title": "Landlord onboarding nudge: daily cron + 24h non-overlapping window",
      "description": "Added a once-daily Vercel Cron (15:00 UTC) hitting /api/cron/landlord-nudge that emails landlords who signed up in the last 24h but have no listings. Chose an exact 24h lookback with no dedup column: a fixed daily run + 24h window tiles signups into non-overlapping windows, so each landlord is caught exactly once (no schema change needed). Accepted edge case: a fully failed run skips that day's cohort. Email copy is the warm 'Having trouble?' variant with reply-to info@useproximity.org as a support channel.",
      "status": "active",
      "context": "PR #59 into staging (feat/landlord-nudge-email). Files: vercel.json, src/app/api/cron/landlord-nudge/route.js, src/lib/email.js (sendLandlordNudgeEmail). Requires CRON_SECRET env var set in Vercel before it runs in prod.",
      "created": "2026-06-07T17:47:47.717Z"
    },
    {
      "id": "decision-1781990747027",
      "title": "Matchmaking algorithm: budget-window + priority-met-at-budget headline + saturation spread",
      "description": "How the proxy-chat matchmaking system works (feat/proxy-chat). PIPELINE: (1) Fixed deterministic question script (questionScript.js QUESTION_PLAN) -> answers become numeric `weights` over 9 dims via WEIGHT_MAP/PRIORITY_WEIGHTS + RANK_BUMPS (steep [1.0,0.5,0.3...] so #1 priority dominates). recomputeFromPreferences (questionEngine.js) re-derives weights identically from a completed prefs object. (2) chatOrchestrator.computeRecommendations -> listingFilter.rankListings. (3) rankListings: buildRankContext builds a DIVERSE pool (spanning price/reviews/proximity/amenities) and computes a deterministic per-listing fit score (computeFitScores) over data dims; Haiku (claude-haiku-4-5, PROXY_CHAT_KEY) ranks lower slots. THE TOP 3 ARE DETERMINISTIC (selectTopThree), not LLM: #1 = the most-EXPENSIVE IN-BUDGET listing that satisfies the student's #1 stated priority (we give them what they asked for at their budget, not what's 'objectively best'); #2/#3 = spinoffs on the SAME priority each leading a different plus (value/amenities/reviews/proximity). KEY POLICIES: budget is a hard WINDOW, not a cheaper-is-better gradient (the `budget` weight dim is excluded from fit; within budget all prices are equal). 'value' = bang-for-buck (quality per $), not absolute cheapness. Proximity is split into campus/med_campus/grocery via proximity_targets (med-year auto-seeds med_campus; free text parsed by REFINE_SYSTEM). Blank listing titles fall back to street address. SATURATION (new): fetchSaturation reads the live DB demand signal (user_listing_interactions 'contacted' = weight 2, prior matchmaking_chat_sessions.recommendations = weight 1) and uses it as a TIE-BREAKER ONLY among near-equal candidates in selectTopThree, so oversubscribed listings get edged out and demand spreads (never overrides priority or busts budget).",
      "status": "active",
      "context": "Files: src/lib/matchmaking/{listingFilter,questionEngine,chatOrchestrator,questionScript}.js, listing-filter.skill.md. Test harnesses (dev-only, reusable): /api/dev/matchmaking-probe + scripts/matchmaking-probe.mjs (bias/concentration); /api/dev/matchmaking-saturation-sim + scripts/matchmaking-saturation-sim.mjs (control vs saturation spread, in-memory). Sim result (150 students): distinct #1 picks 18->30, busiest-listing share 28%->15%, HHI 0.128->0.069, with a FLAT timeline (no penalty to early users). Open follow-ups: optional saturation decay window so filled listings aren't penalized forever; ability to turn saturation strength up past tie-break-only. Not yet pushed; branch feat/proxy-chat.",
      "created": "2026-06-20T21:25:47.027Z"
    },
    {
      "id": "decision-1783008692683",
      "title": "MCP knowledge is committed to git (shared across machines and agents)",
      "description": "mcp/knowledge/ is no longer gitignored (except agent-sessions.json, a machine-local runtime log). Every clone now ships the same repo knowledge, so any contributor's coding agent understands the repo and others' changes without running the MCP first. generate-knowledge.mjs is idempotent (skips rewrites when only the _generated timestamp would change), so regeneration is diff-clean. A CI workflow (.github/workflows/update-knowledge.yml) regenerates and auto-commits drift after pushes to staging/main that touch apps/web/src — this covers contributors who never run the MCP locally. Convention: regenerate and commit knowledge together with code changes; db-schema.json remains hand-maintained against the live DB.",
      "status": "resolved",
      "context": "Branch feat/shared-mcp-knowledge; also updated CLAUDE.md, .gitignore, release-pr-check.yml comment, and domain.json workflow.knowledgeMaintenance text.",
      "created": "2026-07-02T16:11:32.683Z"
    },
    {
      "id": "decision-1783395520566",
      "title": "Matchmaking excludes subleases by default; sublease ≠ sublease_friendly",
      "description": "Proxy matchmaking now excludes sublease listings by default: applySubleasePref (listingFilter.js) drops any listing whose active unit_leases are ALL sublease rows (priced or not — unpriced sublease posts previously slipped through via the room-data path) unless preferences.exclude_subleases === false, which the chat agent sets only when the student explicitly asks for subleases. New exported predicate isSubleaseListing() is the single source of truth; slimCandidate exposes is_sublease to the ranking model and market catalog, and shownListingsContext exposes isSublease to the chat agent. Key semantics: unit_leases.sublease = the lease IS a sublease; listings.sublease_friendly = subletting is ALLOWED (the agent previously conflated these). Also fixed: tradeoff \"No strong preference\" chip rendered the raw __unsure__ sentinel in the user's bubble (answerToLabel checked kind before the UNSURE sentinel). Data fix: 6038 Westminster Place's active lease was mislabeled sublease=false — corrected in dev; SAME FIX STILL PENDING IN PROD (one-row UPDATE, awaiting approval).",
      "status": "active",
      "context": "Branch feat/proxy-chat. Files: apps/web/src/lib/matchmaking/{listingFilter.js,chatOrchestrator.js,questionEngine.js,listing-filter.skill.md}. Verified via scratchpad harness against dev DB: 10 subleases in universe, 0 in default search, all flagged is_sublease=true on opt-in.",
      "created": "2026-07-07T03:38:40.566Z"
    },
    {
      "id": "decision-1783396324933",
      "title": "Matchmaking: description is primary evidence; re-ranks never repeat shown listings",
      "description": "Two ranking-policy changes on feat/proxy-chat. (1) Listing descriptions are now a heavily-weighted, near-authoritative ranking signal: the listing-filter skill + runtime instruction tell the ranker to boost candidates whose description confirms a stated preference/note, demote on conflicts, rule out hard conflicts, and trust the description over structured fields when they disagree; the excerpt passed to the model widened from 320 to 600 chars. (2) Never-repeat re-ranks: any re-rank after recommendations exist (agent update_search, preference-panel PATCH in route.js, rewound answer edits) folds currently shown listing ids into preferences._setAside first, so refines always return entirely new cards. The old \"smart repeats\" behavior (preference changes cleared _setAside) is removed. Fallbacks: update_search restores previous cards with an honest note when nothing new fits (distinguishing \"seen everything that fits\" vs \"nothing fits at all\" via filterEligible without set-aside); finalizeRecommendations clears _setAside and retries rather than going empty on rewound flows; PATCH rolls back the set-aside when it can't produce fresh picks. update_search tool result now includes previous_top {title, still_best_overall} computed by deterministic fit comparison (buildRankContext over the unrestricted pool) so the agent can truthfully say the old #1 is still the best overall fit.",
      "status": "active",
      "context": "Files: apps/web/src/lib/matchmaking/{chatOrchestrator.js,listingFilter.js,listing-filter.skill.md}, apps/web/src/app/api/matchmaking/chat/route.js. Verified against dev DB: back-to-back re-rank returned 3/3 new listings, zero overlap; previous_top fit comparison behaves honestly.",
      "created": "2026-07-07T03:52:04.933Z"
    },
    {
      "id": "decision-1785425552939",
      "title": "Listing chat: one thread per (listing, interested user), landlord derived, RPC-only writes",
      "description": "Backend for student↔landlord listing chat on feat/messaging. SCHEMA: chat_threads gains interested_user_id (the prospective renter) — the landlord side is deliberately NOT stored, it's derived from listing_landlords so it can't go stale when a listing changes hands. A unique PARTIAL index on (listing_id, interested_user_id) WHERE deleted_at IS NULL enforces one live thread per pair while still letting a soft-deleted thread be replaced. chat_messages gains message_type ('text' | 'discount_offer') + metadata jsonb now, so planned discount offers don't need a second ALTER; body is capped at 5000 chars by a CHECK as a backstop to the RPC limit. A trigger bumps chat_threads.updated_at on insert, without which inbox sorting by recent activity is impossible (inserting a message doesn't touch the thread row). ACCESS: all reads and writes go through SECURITY DEFINER RPCs (rpc_start_or_get_listing_chat, rpc_send_chat_message, rpc_mark_thread_read, rpc_list_chat_threads, rpc_get_chat_messages). They trust p_user_id, so EXECUTE is revoked from PUBLIC/anon/authenticated and granted only to service_role — the API routes are the sole callers. rpc_start_or_get_listing_chat takes a pg_advisory_xact_lock so concurrent 'start chat' calls can't race past the unique index. UNREAD: chat_participants.last_read_at drives the flag; a thread reads unread only when the OTHER participant posted after it, and sending marks your own side read. API: /api/chat/threads (GET inbox, POST start-or-reuse), /api/chat/threads/[threadId]/messages (GET paginated, POST reply), /api/chat/threads/[threadId]/read (POST). A non-participant gets a bare 403 rather than a 404, so thread existence isn't discoverable. The pre-existing email-based contactLandlord flow is untouched and still the live path; there is no chat UI yet.",
      "status": "active",
      "context": "Branch feat/messaging (PR into staging). Migrations 202607300001-3, applied to DEV ONLY and recorded in supabase_migrations.schema_migrations — STILL PENDING ON PROD. Files: apps/web/src/app/api/chat/threads/**. Verified by hand against dev: thread reuse returns the same threadId, pagination via before/limit, 400 on bad UUID/limit/body, 403 for non-participants, unread flips true on a landlord message and false after mark-read. Known gap: api-routes.json reports tables:[] for these routes because generate-knowledge.mjs detects tables via .from() only and these use supabase.rpc(), so analyze-impact won't link chat_* migrations back to these endpoints.",
      "created": "2026-07-30T15:32:32.939Z"
    },
    {
      "id": "decision-1785763920000",
      "title": "Listing chat Realtime: hybrid JWT push only; MessagesContext owns open-thread subscribe",
      "description": "Hybrid Realtime on geat/messaging-supabase-realtime (off feat/messaging). WRITES/HISTORY stay on NextAuth + /api/chat/* + service_role SECURITY DEFINER RPCs. PUSH ONLY: browser mints a short-lived HS256 Supabase JWT via GET /api/chat/realtime-token (sub=session.user.id, role/aud=authenticated, ~1h, Legacy JWT secret DEV_/PROD_SUPABASE_JWT_SECRET), then realtime.setAuth + postgres_changes INSERT on chat_messages filtered by thread_id. Channel name chat-thread:${threadId}. No proactive token refresh — reconnect tears down channel, remints, resubscribes. No inbox Realtime (HTTP refreshThreads only). ENV: browser client no longer uses NODE_ENV; next.config.mjs fail-safe-mirrors isProdData() into NEXT_PUBLIC_SUPABASE_URL/DEFAULT_KEY at build time; server.ts + middleware.ts call isProdData() directly. RLS: fn_current_user_id() falls back to auth.uid(); fn_is_chat_participant is SECURITY DEFINER to avoid Realtime policy recursion (migrations 202608030001-2, DEV applied). MessagesContext (Favorites pattern) bootstraps inbox, subscribes only when activeThreadId is set, history-then-subscribe with visibility/SUBSCRIBED refetch safety net, optimistic send with id-dedupe. Mock ChatContext deleted. Verified on DEV: signed-out token 401; landlord receives student INSERT without refresh; non-participant SUBSCRIBED but 0 events; sender Realtime echo once per message id.",
      "status": "active",
      "context": "Branch geat/messaging-supabase-realtime. Files: apps/web/src/app/api/chat/realtime-token/route.js, apps/web/src/lib/chat/realtime.js, apps/web/src/context/MessagesContext.js, apps/web/src/components/layout/Providers.js, apps/web/next.config.mjs, apps/web/src/lib/supabase/{client,server,middleware}.ts, supabase/migrations/202608030001_chat_realtime_auth.sql, 202608030002_chat_realtime_rls_fix.sql. Chat schema/RPC migrations 202607300001-3 + realtime migrations still DEV ONLY — pending prod. No chat UI yet (MessagesContext data/actions only).",
      "created": "2026-08-03T13:32:00.000Z"
    }
  ],
  "bugs": [
    {
      "id": "bug-1785425552940",
      "title": "analyze-impact is monorepo-blind: reports 0 src changes for every apps/web change",
      "description": "mcp/src/impact.mjs still assumes the pre-monorepo layout: ROOT is the repo root and SRC = <repo>/src, which no longer exists (code moved to apps/web/src). analyzeImpact() filters changed files with f.startsWith(\"src/\"), but git reports apps/web/src/..., so every source change is bucketed as 'other' and the affected-endpoints/pages sections come back empty. Observed on feat/messaging: 3 new API route files reported as '0 src, 3 migration, 3 other' with 'Affected API endpoints: None'. generate-knowledge.mjs was made monorepo-aware in June (REPO_ROOT + ROOT=apps/web) but impact.mjs was not updated to match. Consequence: release-pr-check.yml's impact analysis and run-impact-tests have likely been silently skipping all code-driven surfaces since the monorepo move — only the migration→table fan-out still produces results, which is why unrelated endpoints get tested instead. Fix is to point SRC at apps/web/src and rebase the changed-file filter (and rel()) on that prefix.",
      "status": "open",
      "context": "Found during Stage 4 verification of feat/messaging. Not fixed there (out of scope for the chat PR). Reproduce: node -e with analyzeImpact({base:'staging'}) at repo root.",
      "created": "2026-07-30T15:32:32.940Z"
    }
  ],
  "migrations": []
}